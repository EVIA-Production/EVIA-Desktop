
import React from 'react';

interface ChatStatusProps {
  chatId: string | null;
}

const ChatStatus: React.FC<ChatStatusProps> = ({ chatId }) => {
  // Not displaying any content
  return null;
};

export default ChatStatus;
